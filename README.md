# 🏦 Currency Converter API
Backend-приложение для конвертации валют, выступающее прослойкой между клиентскими приложениями и внешними API курсов валют.

## 🚀 Возможности
📊 Получение списка поддерживаемых валют

💹 Конвертация валют по актуальным курсам
👤 Управление пользовательскими настройками
🍪 Автоматическая аутентификация по cookies
⚡ Многоуровневое кэширование для оптимизации
📚 Swagger документация API

## 🛠 Технологии
Node.js (LTS) - серверная платформа
TypeScript - язык программирования
Express - веб-фреймворк
Supabase - база данных (PostgreSQL)
Axios - HTTP-клиент для внешних API
Swagger - документация API
Node-Cache - кэширование в памяти

## 📦 Установка и запуск
1. Предварительные требования Node.js версии 18.17.0 или выше
2. Аккаунт в Supabase

### 1. Клонирование и установка зависимостей

git clone <repository-url>
cd currency-converter

npm install

### 2. Настройка окружения
Создайте файл .env на основе .env.example:

PORT=3000
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
EXCHANGE_RATE_API_KEY=optional_exchange_api_key
NODE_ENV=development

### 3. Настройка базы данных
В Supabase создайте таблицу user_settings:

sql
CREATE TABLE user_settings (
  user_id TEXT PRIMARY KEY,
  base_currency TEXT NOT NULL DEFAULT 'USD',
  favorites TEXT[] DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

### 4. Запуск приложения

// Режим разработки (с hot-reload)
npm run dev

// Или production сборка
npm run build
npm start

После запуска приложение будет доступно по адресу: http://localhost:3000

## 📡 API Endpoints
### 🏠 Главная страница
GET / - Информация о API и доступных эндпоинтах

### 📊 Валюты
GET /api/currencies - Получить список поддерживаемых валют

Пример ответа:

json
{
  "currencies": ["USD", "EUR", "GBP", "JPY", "CAD"]
}
### 💹 Курсы валют
GET /api/rates?base=USD&targets=EUR,GBP - Получить курсы валют

Параметры:

base (опционально) - базовая валюта (по умолчанию из настроек пользователя или USD)

targets (обязательно) - целевые валюты через запятую

Пример ответа:

json
{
  "base": "USD",
  "rates": {
    "EUR": 0.85,
    "GBP": 0.73
  },
  "timestamp": 1699456789012
}
### 👤 Пользователь
GET /api/user - Получить настройки пользователя

POST /api/user - Обновить настройки пользователя

Пример тела запроса для POST:

json
{
  "base_currency": "EUR",
  "favorites": ["USD", "GBP", "JPY"]
}
Пример ответа:

json
{
  "user_id": "uuid-пользователя",
  "base_currency": "USD",
  "favorites": ["EUR", "GBP"],
  "created_at": "2023-11-08T10:00:00.000Z",
  "updated_at": "2023-11-08T15:30:00.000Z"
}
## 📚 Документация
GET /api-docs - Swagger документация API

## 🔐 Аутентификация
Приложение использует автоматическую аутентификацию:

При первом обращении выдается httpOnly cookie user_id

Пользователь автоматически создается в базе данных

Все последующие запросы идентифицируются по cookie

## ⚡ Кэширование
Приложение использует трехуровневое кэширование:

🕐 1 час - Список поддерживаемых валют

🕐 24 часа - Курсы валютных пар

🕐 5 минут - Результаты запросов пользователей

## 🗂 Структура проекта
text
currency-converter/
├── src/
│   ├── controllers/     // Обработчики запросов
│   ├── services/        // Бизнес-логика и внешние API
│   ├── middleware/      // Промежуточное ПО
│   ├── models/          // TypeScript интерфейсы
│   ├── routes/          // Маршруты API
│   └── app.ts           // Главный файл приложения
├── dist/                // Скомпилированный код
├── demo/                // Демонстрационные материалы
└── config files         // Конфигурационные файлы
## 🧪 Тестирование
После запуска проверьте работу API:

Откройте в браузере: http://localhost:3000/api-docs

Протестируйте эндпоинты через Swagger UI

## 🔧 Скрипты
npm run dev - Запуск в режиме разработки

npm run build - Сборка TypeScript в JavaScript

npm start - Запуск production версии

npm run lint - Проверка кода с ESLint

## 🤝 Разработка
Требования к коду
Используйте TypeScript для типизации

Следуйте правилам ESLint

Добавляйте Swagger документацию для новых эндпоинтов

Добавление нового эндпоинта
Создайте интерфейс в src/models/

Добавьте метод в сервис в src/services/

Создайте контроллер в src/controllers/

Добавьте маршрут в src/routes/

Обновите Swagger документацию

## 🎯 Code Quality

### ESLint Configuration

The project uses ESLint with TypeScript support for code quality assurance.

**Available commands:**

npm run lint      // Check code for errors
npm run lint:fix  // Automatically fix errors  
npm run lint:watch // Watch mode for development

## 📝 Лицензия
Этот проект создан в рамках тестового задания.

## 📞 Поддержка
При возникновении проблем:

Проверьте настройки окружения в .env файле

Убедитесь, что база данных Supabase настроена правильно

Проверьте логи приложения в консоли

Убедитесь, что RLS отключен или правильно настроен
